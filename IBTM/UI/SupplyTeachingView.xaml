<UserControl x:Class="IBTM.UI.SupplyTeachingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:device="clr-namespace:IBTM.Device;assembly=IBTM.Device"
             xmlns:ui="clr-namespace:IBTM.UI"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=ui:SupplyTeachingViewModel, IsDesignTimeCreatable=False}"
             Background="{StaticResource AppBgBrush}">

    <UserControl.Resources>
        <CollectionViewSource x:Key="GroupedPoints" Source="{Binding Points}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Section"/>
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>

    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,16">
            <TextBlock Text="PCB Supply Teaching"
                       Style="{StaticResource TitleStyle}"/>
            <TextBlock Text="Teach every supply and shared-buffer position."
                       Style="{StaticResource PageSubtitleStyle}"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Border Style="{StaticResource CardStyle}"
                        Padding="18"
                        Margin="0,0,0,12">
                    <StackPanel>
                        <Grid Margin="0,0,0,14">
                            <TextBlock Text="Current position"
                                       Style="{StaticResource PanelTitleStyle}"/>
                            <TextBlock Text="{Binding ActiveMotionGroup, Converter={StaticResource EnumDescriptionConverter}}"
                                       HorizontalAlignment="Right"
                                       FontFamily="Cascadia Mono, Consolas"
                                       FontSize="10"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="X"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           FontSize="10"/>
                                <TextBlock Text="{Binding CurrentX, StringFormat=F3}"
                                           FontFamily="Cascadia Mono, Consolas"
                                           FontSize="23"
                                           Foreground="{StaticResource TextPrimaryBrush}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1"
                                        Visibility="{Binding HasY, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="Y"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           FontSize="10"/>
                                <TextBlock Text="{Binding CurrentY, StringFormat=F3}"
                                           FontFamily="Cascadia Mono, Consolas"
                                           FontSize="23"
                                           Foreground="{StaticResource TextPrimaryBrush}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Z"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           FontSize="10"/>
                                <TextBlock Text="{Binding CurrentZ, StringFormat=F3}"
                                           FontFamily="Cascadia Mono, Consolas"
                                           FontSize="23"
                                           Foreground="{StaticResource TextPrimaryBrush}"/>
                            </StackPanel>
                        </Grid>

                        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                            <TextBlock Text="{Binding HorizontalZTarget, Converter={StaticResource EnumDescriptionConverter}}"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock Text="{Binding HorizontalZ, StringFormat={}{0:F3} mm}"
                                       Margin="6,0,0,0"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       FontFamily="Cascadia Mono, Consolas"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}"
                        Padding="18"
                        Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Jog"
                                   Style="{StaticResource PanelTitleStyle}"
                                   Margin="0,0,0,12"/>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="36"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="X"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                            <ui:HoldButton Grid.Column="1"
                                           Content="-"
                                           Style="{StaticResource JogButtonStyle}"
                                           PressCommand="{Binding JogXMinusCommand}"
                                           ReleaseCommand="{Binding JogStopCommand}"/>
                            <ui:HoldButton Grid.Column="3"
                                           Content="+"
                                           Style="{StaticResource JogButtonStyle}"
                                           PressCommand="{Binding JogXPlusCommand}"
                                           ReleaseCommand="{Binding JogStopCommand}"/>

                        </Grid>

                        <Grid Margin="0,0,0,8"
                              Visibility="{Binding HasY, Converter={StaticResource BoolToVis}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="36"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Y"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                            <ui:HoldButton Grid.Column="1"
                                           Content="-"
                                           Style="{StaticResource JogButtonStyle}"
                                           PressCommand="{Binding JogYMinusCommand}"
                                           ReleaseCommand="{Binding JogStopCommand}"/>
                            <ui:HoldButton Grid.Column="3"
                                           Content="+"
                                           Style="{StaticResource JogButtonStyle}"
                                           PressCommand="{Binding JogYPlusCommand}"
                                           ReleaseCommand="{Binding JogStopCommand}"/>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="36"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Z"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                            <ui:HoldButton Grid.Column="1"
                                           Content="-"
                                           Style="{StaticResource JogButtonStyle}"
                                           PressCommand="{Binding JogZMinusCommand}"
                                           ReleaseCommand="{Binding JogStopCommand}"/>
                            <ui:HoldButton Grid.Column="3"
                                           Content="+"
                                           Style="{StaticResource JogButtonStyle}"
                                           PressCommand="{Binding JogZPlusCommand}"
                                           ReleaseCommand="{Binding JogStopCommand}"/>
                        </Grid>

                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Content="STOP"
                                    Command="{Binding JogStopCommand}"
                                    Style="{StaticResource JogStopButtonStyle}"/>
                            <Button Grid.Column="2"
                                    Content="{Binding HorizontalZTarget, Converter={StaticResource EnumDescriptionConverter}}"
                                    Command="{Binding MoveToHorizontalZCommand}"
                                    Style="{StaticResource SecondaryButtonStyle}"/>
                        </Grid>

                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    Margin="0,10,0,0">
                            <TextBlock Text="Jog speed"
                                       FontSize="10"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>
                            <ComboBox ItemsSource="{Binding JogSpeeds}"
                                      SelectedItem="{Binding JogSpeed, Mode=TwoWay}"
                                      MinWidth="72"
                                      Padding="8,5"/>
                            <TextBlock Text="mm/s"
                                       FontSize="10"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       VerticalAlignment="Center"
                                       Margin="6,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}" Padding="18">
                    <StackPanel>
                        <TextBlock Text="Buffer setup"
                                   Style="{StaticResource PanelTitleStyle}"
                                   Margin="0,0,0,10"/>
                        <TextBlock Text="1  Teach Supply Rotation Z."
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,0,0,5"/>
                        <TextBlock Text="2  Teach Supply Place and Clear Z."
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,5"/>
                        <TextBlock Text="3  Teach Placement and both collision boundaries."
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,5"/>
                        <TextBlock Text="4  Save the complete buffer setup."
                                   Foreground="{StaticResource TextMutedBrush}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <Border Grid.Column="2"
                    Style="{StaticResource CardStyle}"
                    Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Margin="0,0,0,14">
                        <TextBlock Text="Positions"
                                   Style="{StaticResource PanelTitleStyle}"/>
                        <TextBlock Text="Selecting a position also selects the handler controlled by Jog."
                                   FontSize="11"
                                   Foreground="{StaticResource TextMutedBrush}"
                                   Margin="0,5,0,0"/>
                        <WrapPanel Margin="0,12,0,0">
                            <Button Command="{Binding ToggleActuatorCommand}"
                                    CommandParameter="{x:Static device:OutputIo.PcbSupplyRotate}"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    MinWidth="170"
                                    Margin="0,0,8,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Source={x:Static device:OutputIo.PcbSupplyRotate}, Converter={StaticResource EnumDescriptionConverter}}"/>
                                    <TextBlock Text="{Binding SupplyRotation, Converter={StaticResource EnumDescriptionConverter}}"
                                               Margin="10,0,0,0"
                                               Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding ToggleActuatorCommand}"
                                    CommandParameter="{x:Static device:OutputIo.PcbSupplyIpmFixerForward}"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    MinWidth="170"
                                    Margin="0,0,8,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Source={x:Static device:OutputIo.PcbSupplyIpmFixerForward}, Converter={StaticResource EnumDescriptionConverter}}"/>
                                    <ContentControl Tag="{Binding SupplyIpmFixed}"
                                                    Style="{StaticResource IoInputStateStyle}"
                                                    Margin="10,0,0,0"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding ToggleActuatorCommand}"
                                    CommandParameter="{x:Static device:OutputIo.PcbPlacementIpmGripperClose}"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    MinWidth="180">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Source={x:Static device:OutputIo.PcbPlacementIpmGripperClose}, Converter={StaticResource EnumDescriptionConverter}}"/>
                                    <ContentControl Tag="{Binding PlacementIpmGripperClosed}"
                                                    Style="{StaticResource IoInputStateStyle}"
                                                    Margin="10,0,0,0"/>
                                </StackPanel>
                            </Button>
                        </WrapPanel>
                        <WrapPanel Margin="0,8,0,0">
                            <TextBlock Text="{Binding Source={x:Static device:InputIo.PcbSupplyPcbDetected}, Converter={StaticResource EnumDescriptionConverter}}"
                                       VerticalAlignment="Center"/>
                            <ContentControl Tag="{Binding SupplyPcbDetected}"
                                            Style="{StaticResource IoInputStateStyle}"
                                            Margin="8,0,24,0"/>
                            <TextBlock Text="{Binding Source={x:Static device:InputIo.PcbPlacementPcbDetected}, Converter={StaticResource EnumDescriptionConverter}}"
                                       VerticalAlignment="Center"/>
                            <ContentControl Tag="{Binding PlacementPcbDetected}"
                                            Style="{StaticResource IoInputStateStyle}"
                                            Margin="8,0,0,0"/>
                        </WrapPanel>
                    </StackPanel>

                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding Source={StaticResource GroupedPoints}}"
                             SelectedItem="{Binding SelectedPoint}"
                             Background="Transparent"
                             BorderThickness="0"
                             ItemContainerStyle="{StaticResource FlatListItemStyle}"
                             HorizontalContentAlignment="Stretch">
                        <ListBox.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name, Converter={StaticResource EnumDescriptionConverter}}"
                                                   Style="{StaticResource SectionHeaderStyle}"
                                                   Margin="2,14,0,8"/>
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                            </GroupStyle>
                        </ListBox.GroupStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{StaticResource CardBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="14,12"
                                        Margin="0,0,0,8">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background"
                                                    Value="{StaticResource EquipBgBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsMouseOver,
                                                                              RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                                             Value="True">
                                                    <Setter Property="Background" Value="#181D23"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsSelected,
                                                                              RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                                             Value="True">
                                                    <Setter Property="Background" Value="#252B34"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="190"/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="60"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding Name}"
                                                   FontFamily="Cascadia Mono, Consolas"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding MotionGroup, Converter={StaticResource EnumDescriptionConverter}}"
                                                   FontSize="10"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding Storage, Converter={StaticResource EnumDescriptionConverter}}"
                                                   Foreground="{StaticResource TextMutedBrush}"
                                                   VerticalAlignment="Center"/>
                                        <Border Grid.Column="3"
                                                Background="{StaticResource CardBgBrush}"
                                                CornerRadius="4"
                                                Padding="8,3"
                                                HorizontalAlignment="Left">
                                            <TextBlock Text="{Binding TeachMode, Converter={StaticResource EnumDescriptionConverter}}"
                                                       FontFamily="Cascadia Mono, Consolas"
                                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                                        </Border>
                                        <TextBlock Grid.Column="4"
                                                   Text="{Binding PositionLabel}"
                                                   FontFamily="Cascadia Mono, Consolas"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   HorizontalAlignment="Right"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <WrapPanel Grid.Row="2"
                               HorizontalAlignment="Right"
                               Margin="0,14,0,0">
                        <Button Content="Move To"
                                Command="{Binding MoveToPointCommand}"
                                Style="{StaticResource SecondaryButtonStyle}"
                                MinWidth="90"
                                Margin="0,0,8,0"/>
                        <Button Content="Teach Selected"
                                Command="{Binding TeachCurrentPositionCommand}"
                                Style="{StaticResource SecondaryButtonStyle}"
                                MinWidth="110"
                                Margin="0,0,8,0"/>
                        <Button Content="Save Buffer Setup"
                                Command="{Binding SaveBufferSetupCommand}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                MinWidth="140"/>
                    </WrapPanel>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2"
                Style="{StaticResource CardStyle}"
                Padding="10"
                Margin="0,16,0,0">
            <WrapPanel VerticalAlignment="Center">
                <TextBlock Text="Recipe"
                           FontSize="10"
                           Foreground="{StaticResource TextMutedBrush}"
                           VerticalAlignment="Center"
                           Margin="0,0,8,0"/>
                <TextBox Text="{Binding RecipeEditor.Name, UpdateSourceTrigger=PropertyChanged}"
                         MinWidth="120"
                         FontSize="11"
                         Margin="0,0,8,0"/>
                <Button Content="New"
                        Command="{Binding RecipeEditor.NewCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        MinWidth="60"
                        Margin="0,0,4,0"/>
                <Button Content="Save"
                        Command="{Binding RecipeEditor.SaveCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        MinWidth="60"
                        Margin="0,0,4,0"/>
                <ComboBox ItemsSource="{Binding RecipeEditor.Recipes}"
                          MinWidth="180"
                          Margin="0,0,8,0"
                          SelectionChanged="RecipeFile_Selected"/>
            </WrapPanel>
        </Border>
    </Grid>
</UserControl>
